description = "Ініціалізувати новий трек для фічі або багфіксу"

prompt = """
You are initiating a new SDLC Track for: {{args}}

## Step 1: Dispatch BA
Call @ba with the task: "Initialize a new Conductor track for '{{args}}'. Analyze requirements, choose the appropriate Pipeline (Standard Feature or Bug Fix), and create the track directory with spec.md and plan.md."

## Step 2: Handoff
Once the BA creates the track:
- If Pipeline is 'Standard Feature': Call @ddd-architect to define the architecture.
- If Pipeline is 'Bug Fix': Call @debugger to find the root cause.

## Step 3: Confirm
Show the link to the new `plan.md` to the user and announce that orchestration has begun.
"""
